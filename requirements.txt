fastapi>=0.110
uvicorn>=0.29
pymupdf>=1.24
pillow>=10.0
httpx>=0.27
python-dotenv>=1.0
python-multipart>=0.0.9
bcrypt>=4.0
openpyxl>=3.1
reportlab>=4.0
pytrends>=4.9
dbfread>=2.0.7
cryptography>=42.0
qrcode>=7.4
zeroconf>=0.131

# v8.14.0: Production-hardening dependencies
# - Cloud backup via Google Drive (OAuth + Drive API)
google-auth>=2.28
google-auth-oauthlib>=1.2
google-api-python-client>=2.100
# - Data at rest encryption (SQLCipher, AES-256-CBC + HMAC-SHA256)
#   Optional: if not installed, app falls back to plaintext SQLite (with a warning).
sqlcipher3-binary>=0.5.0; platform_system == "Windows" or platform_system == "Darwin"
# - Daily sales digest via Twilio WhatsApp API
twilio>=8.10
