fastapi>=0.110
uvicorn>=0.29
pymupdf>=1.24
pillow>=10.0
httpx>=0.27
python-dotenv>=1.0
python-multipart>=0.0.9
bcrypt>=4.0
openpyxl>=3.1
reportlab>=4.0
pytrends>=4.9
dbfread>=2.0.7
cryptography>=42.0
qrcode>=7.4
zeroconf>=0.131

# v8.14.0: Production-hardening dependencies
# - Cloud backup via Google Drive (OAuth + Drive API)
google-auth>=2.28
google-auth-oauthlib>=1.2
google-api-python-client>=2.100
# - Data at rest encryption (SQLCipher, AES-256-CBC + HMAC-SHA256)
#   Optional: if not installed, app falls back to plaintext SQLite (with a warning).
#   v8.15.1 FIX: sqlcipher3-binary publishes ONLY manylinux wheels on PyPI
#   (zero Windows/macOS wheels across all 13 releases - verified), so the old
#   marker `platform_system == "Windows" or platform_system == "Darwin"` could
#   never install and aborted `pip install -r requirements.txt` on Windows.
#   Linux-only now. Windows builds run with standard SQLite by design
#   (graceful fallback in app/db.py), and the build script prints a NOTE.
sqlcipher3-binary>=0.5.0; platform_system == "Linux"
# - Daily sales digest via Twilio WhatsApp API
twilio>=8.10
